#!/usr/bin/env bash
# bin/use <build-dir>

set -e

# Syntax sugar.
indent() {
  RE="s/^/       /"
  [ $(uname) == "Darwin" ] && sed -l "$RE" || sed -u "$RE"
}

puts-step "Installing gem dependencies"
bundle exec bundle install --local --without test development 2>&1 | indent
